server:
  port: 8085

spring:
  application:
    name: admin-server

eureka:
  client:
    service-url:
      defaultZone: http://host.docker.internal:8761/eureka/
  instance:
    prefer-ip-address: true

